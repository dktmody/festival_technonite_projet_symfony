{% extends 'base.html.twig' %}

{% block title %}Hello ArtisteController!{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center mt-5 mb-5" >
        {% for categorie in categories %}      
            <button type="button" class="btn btn-outline-primary m-1">{{categorie.name}}</button>
        {% endfor %}
    </div>

<div class="row">
{% for categorie in categories %}
{% for artiste in artistes %}

    <div class="col-4">
        <div class="card border-{{categorie.id}} mb-3" >
            <div class="card-body">
                <div class="card-header text-end"><span class="badge rounded-pill bg-secondary">Music de rue</span></div>
                <h4 class="card-title">DJ {{artiste.name}}</h4>
                <p class="card-text">{{artiste.description | slice(1, 100)}}</p>
                <p><a href="#">Découvrir l'artiste</a></p>
            </div>
        </div>  
     </div>
{% endfor %}
{% endfor %}

</div>






{% endblock %}
